version: 2.1
jobs:
  build-and-test:
    docker:
      - image: mcr.microsoft.com/dotnet/core/sdk:3.1

    steps:
      - checkout
      - run: ls
      - run: dotnet build
      - run: dotnet test

workflows:
  build-and-test:
    jobs:
      - build-and-test